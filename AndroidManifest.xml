<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tvarkarastis">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Button">
        <activity android:name="com.example.tvarkarastis.Kursopasirinkimas"></activity>
        <activity android:name="com.example.tvarkarastis.registerActivity" />
        <activity android:name="com.example.tvarkarastis.recyclerView" />
        <activity android:name="com.example.tvarkarastis.scheduleActivity" />
        <activity android:name="com.example.tvarkarastis.secondactivity" />
        <activity android:name="com.example.tvarkarastis.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>